{
  "name": "politeshop",
  "module": "index.ts",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "chrome-types": "^0.1.303",
    "esbuild": "^0.23.1"
  },
  "prettier": {
    "printWidth": 100
  },
  "scripts": {
    "build": "esbuild src/content.ts src/worker.ts --bundle --minify --outdir=build && cp -r static build && cp manifest.json build"
  }
}